import Authenticate from "@/components/authenticate";
import React from "react";

function Login() {
  return (
    <main className="flex items-center justify-center w-full relative h-full">
      <Authenticate />
    </main>
  );
}

export default Login;
